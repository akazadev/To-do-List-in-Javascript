export const taskContainer = [];

export class task{
    constructor(name, tag){
        this.nom = this.nom;
        this.tag = tag;
    }
    
    addTask(task){
        taskContainer.push(task)
    }
}